# multi-port-manual-switch.40

- 同一国家、同一协议允许使用不同端口创建多条独立出口，唯一限制改为端口不能重复。
- 节点名称加入端口，例如“236.日本-HY2-7866-20260905”，避免同协议多线路重名。
- 合并重复的前端保存函数，可靠保留用户手动选中的候选 IP。
- 仅修改策略或手选 IP 时不再重建 3x-ui 入站，并立即唤醒出口进程切换当前线路。
- 手选不可用或不符合 IP 类型策略的节点时给出明确提示，不再静默回退。

# multi-protocol-country.39

- 同一个国家允许创建多条不同协议的出口，例如日本 HY2、日本 VLESS 和日本 Trojan。
- 添加通道时不再隐藏已经建立过线路的国家，并提示该国家已经使用的协议。
- 同一国家的同一协议仍禁止重复创建，所有通道必须使用不同端口。
- 客户端名称加入协议以避免重名，例如“236.日本-HY2-20260905”和“236.日本-VLESS-20260905”。

# server-suffix-names.38

- 所有客户端节点名称增加当前服务器公网 IPv4 的最后一段，例如“236.服务器直连”。
- 国家节点显示为“服务器尾号.中文国家-建立日期”，例如“236.日本-20260905”。
- 单节点链接和总订阅使用同一命名规则，多个 VPS 导入同一客户端时可以直接区分。
- 升级时自动迁移已有直连及国家名称，不改变端口、订阅 ID 或客户端凭据。

# chinese-node-names.37

- 直连节点显示名统一为“服务器直连”。
- 新建国家节点统一使用“中文国家名-建立日期”，例如“日本-20260905”。
- 启动时迁移已有的“美国线路”“韩国线路”及“Country-日期”等旧名称。
- 旧名称中带有日期时保留原建立日期，订阅地址、端口和客户端凭据不变。

# clipboard-copy.36

- 节点链接及总订阅按钮自动复制并显示“已复制到剪贴板”。
- HTTP 后台使用兼容复制方式，HTTPS 优先使用浏览器剪贴板接口。
- 浏览器拒绝所有复制方式时保留手动复制提示，失败不会显示复制成功。

# bootstrap-dashboard.35

- 每 5 秒发现安装后创建的待检测国家通道，自动启动首次检测；同一通道只保留一个任务。
- 修复新安装时后台先启动、国家配置后创建导致默认国家永久等待的问题。
- 直连、订阅与国家出口分区显示，国家卡片按连接状态着色，明确显示等待与恢复原因。
- 配置字段增加标签，入口与出口服务商分别显示，适配窄屏。
- 提供 upgrade-dashboard.sh，备份后只更新后台程序，不重建入站或订阅。

# xui-template-init.34

- 兼容 3x-ui 3.7.0 新安装数据库不含 `xrayTemplateConfig` 的情况；
- 缺少设置时自动写入 3x-ui v3 官方基础 Xray 模板；
- 同时修复首次直连节点配置和后续多国家线路配置；
- 已有 Xray 模板仍采用增量修改，不覆盖用户现有配置。

# open-host-firewall.33

- Ubuntu/Debian 一键安装默认关闭 UFW 与 netfilter-persistent；
- IPv4/IPv6 的 INPUT、FORWARD、OUTPUT 默认策略统一设为 ACCEPT；
- 清空本机 filter 表规则，避免 Oracle Ubuntu 镜像规则阻止证书和管理端口；
- 可在运行安装器前设置 `OPEN_ALL_PORTS=0` 保留本机防火墙；
- 明确提示 Oracle Cloud VCN、安全列表和 NSG 仍需在云控制台配置。

# installer-certificate.32

- 在申请证书前开放服务器本机所需 TCP/UDP 端口；
- 3x-ui 首次申请证书失败后自动进行第二次签发；
- 区分“本机端口已开放”和“Oracle Cloud 公网入口不可达”，提供准确提示；
- 修复 `sudo ml credentials` 的命令语法错误；
- 新增 `sudo ml info`，集中显示两个后台、订阅、证书与服务状态；
- 安装成功或证书中断时均在终端末尾显示当前可用登录信息。

# stability.31

本版本首次把当前定制的多国家独立出口系统完整发布到一键安装仓库，并加入低内存稳定性修复。

## 安装与升级

- 新增统一安装向导：节点管理、3x-ui、入站、出站、路由、证书和订阅一次完成；
- 支持 VLESS、Trojan和 Hysteria2；
- 支持公网 IPv4短期证书及自动续签；
- 升级前自动备份程序文件和3x-ui数据库；
- 保留已有后台账号、节点资料和国家线路设置。

## 多国家出口

- 每条国家线路使用独立网络命名空间和独立 VPNGate出口；
- 支持按国家、协议、端口和IP类型独立管理；
- 严格国家锁定，失败时不会串到其他国家；
- 添加或删除国家线路时同步创建或删除对应3x-ui入站和路由。

## 稳定性修复

- 保存经过完整联网、国家及IP类型验证的出口记录；
- 优先复用最近验证并稳定连接过的节点；
- 三个公网IP检测地址容错；
- 增加同国家候选尝试数量，同时缩短单节点失败等待；
- 清理遗留 OpenVPN测试进程；
- 降低批量测试时节点文件写入频率。

## 客户端兼容性

- Hysteria2链接明确输出 TLS、SNI和严格证书验证参数；
- 移除容易令新版 v2rayNG误判为不安全连接的旧 ECH及 insecure参数；
- 同时生成通用节点/订阅和 Clash/Mihomo配置。
